---
phase: 01-prove-the-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/wall-shell.ts
  - dev/harness.html
  - dev/harness-check.mjs
  - dist/moran-family-board-card.js
  - .planning/phases/01-prove-the-foundation/01-04-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - FOUND-02
must_haves:
  truths:
    - "Every visible wall Day person avatar renders as a true 40x40 circle instead of a vertically compressed ellipse, both beside an optional badge and without one."
    - "The wall Day person header remains 80px high and contains its 40px avatar, visible name/status, and optional 48px badge without horizontal or vertical overflow."
    - "The same production bundle keeps legacy person avatars at their existing 34x34 geometry and preserves the legacy root and interaction path when `layout` is absent."
    - "The deterministic 1920x1080 America/Chicago browser gate fails when either wall or legacy person-avatar width and height diverge."
  artifacts:
    - path: "src/wall-shell.ts"
      provides: "Wall-scoped square avatar geometry and 80px person-header layout with dedicated tracks for avatar, text, and optional badge"
      contains: ".moran-wall-shell .phead"
    - path: "dev/harness.html"
      provides: "Rendered wall and legacy avatar dimension and header-containment assertions"
    - path: "dev/harness-check.mjs"
      provides: "Headless Chrome enforcement and reporting for both avatar geometry baselines"
    - path: "dist/moran-family-board-card.js"
      provides: "Rollup-generated HACS bundle containing the corrected geometry"
  key_links:
    - from: "src/wall-shell.ts"
      to: "src/ha-family-board-card.ts"
      via: "wall-rooted selectors fit the existing Day `.phead .avatar` markup into explicit avatar, text, and badge grid tracks without changing shared legacy styles"
      pattern: "moran-wall-shell.*phead|avatar"
    - from: "dev/harness.html"
      to: "dist/moran-family-board-card.js"
      via: "both wall and legacy scenarios measure DOMRects rendered by the compiled custom element"
      pattern: "dist/moran-family-board-card.js"
    - from: "dev/harness-check.mjs"
      to: "dev/harness.html"
      via: "the loopback Chrome runner requires scenario-specific avatar evidence before reporting success"
      pattern: "harness-check-result"
---

## Phase Goal

**As a** Home Assistant administrator, **I want to** evaluate an opt-in wall-mode day slice with correctly rendered person identity, **so that** the foundation is visually credible without changing existing configurations or behavior.

<objective>
Close the diagnosed Phase 1 UAT gap by making wall Day person avatars intrinsically square and non-shrinking within the opt-in wall shell, fitting the existing wall header contents around that invariant, and proving both wall and legacy geometry through the production-bundle browser harness.

Purpose: Restore the failed visual acceptance truth without widening Phase 1 into the deferred person-filter, now/next, density, or responsive redesign work.
Output: Corrected wall-scoped avatar and person-header CSS, deterministic rendered-geometry coverage for wall and legacy scenarios, a regenerated distribution bundle, and execution evidence.
</objective>

<execution_context>
@/Users/emiliano/.claude/gsd-core/workflows/execute-plan.md
@/Users/emiliano/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-prove-the-foundation/01-CONTEXT.md
@.planning/phases/01-prove-the-foundation/01-UI-SPEC.md
@.planning/phases/01-prove-the-foundation/01-UAT.md
@.planning/debug/wall-avatar-not-fully-round.md
@.planning/phases/01-prove-the-foundation/01-03-SUMMARY.md
@src/ha-family-board-card.ts
@src/wall-shell.ts
@dev/harness.html
@dev/harness-check.mjs
@package.json

<interfaces>
The diagnosed runtime measurements are authoritative for this closure: every wall `.phead .avatar` is currently 40x19.5px because `.phead` is a fixed-height column flex container and `.avatar` retains `flex-shrink: 1`; legacy renders the same shared avatar contract at 34x34px because its header expands to content. Correct that wall-only constraint under `.moran-wall-shell` and leave the unscoped `.avatar` rule unchanged. The first wall fixture header includes a `.pbadge` whose inherited wall interaction contract is at least 48x48px; the other three headers exercise the no-badge path.

Preserve these existing public and behavioral boundaries:

- `_avatar()` continues to render the same `.avatar` / `.avatar.initials` markup and data; this plan changes geometry, not identity or event behavior.
- `.moran-wall-shell .phead` remains 80px high with 8px vertical and 16px horizontal padding, leaving a 64px content box.
- `.pbadge` remains independently focusable, pointer/Enter/Space operable, and at least 48x48px.
- `dev/harness.html` imports only `dist/moran-family-board-card.js`; wall and legacy continue using the same generic mocked Home Assistant data path and fixed clock.
- Exact `layout: wall` remains the sole wall opt-in. The no-layout legacy branch, custom-element names, config contract, calendar reads, and event routing remain unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve square avatars and fit the 80px wall person header</name>
  <files>src/wall-shell.ts</files>
  <read_first>
    - `.planning/phases/01-prove-the-foundation/01-UAT.md`
    - `.planning/debug/wall-avatar-not-fully-round.md`
    - `.planning/phases/01-prove-the-foundation/01-UI-SPEC.md`
    - `src/ha-family-board-card.ts`
    - `src/wall-shell.ts`
  </read_first>
  <action>Implement the diagnosed geometry correction without changing markup, interaction handlers, or the shared unscoped `.avatar` rule in `src/ha-family-board-card.ts`. In `src/wall-shell.ts`, add the square/non-shrinking geometry only beneath `.moran-wall-shell` for the Day person-header avatar: retain token-driven `width` and `height`, set `aspect-ratio: 1 / 1`, and prevent flex shrink within that wall-scoped selector. Do not place `flex-shrink`, `aspect-ratio`, fixed bases, or minimums on the shared `.avatar` rule because focus, timeline, week, compact, and every no-layout legacy context consume that rule with their own sizing.

Preserve the Phase 1 80px header contract while replacing the visible wall `.phead`'s single vertical stack with a wall-scoped compact grid. For `.moran-wall-shell .phead:not(.off)`, use three columns—`var(--fb-avatar-size)`, `minmax(0, 1fr)`, and `auto`—and two content rows inside the existing 64px content box. Place the avatar in column 1 spanning both rows, place `.pname` and `.pstatus` in column 2 on rows 1 and 2, and place `.pbadges` in column 3 spanning both rows. Center all three tracks vertically; give the text column `min-width: 0` plus single-line ellipsis so long fixture/configured identity cannot force header overflow. Remove the wall badge container's top margin and prevent the single optional fixture badge from wrapping; retain the existing 48px interaction minimum and independent badge behavior. With no `.pbadges` node, the third `auto` column must collapse naturally. Keep an `.off` wall header on its inherited 48px collapsed width, set `padding-inline: 0` and center its sole avatar so the existing 1px divider cannot cause overflow, and do not show or reserve text/badge tracks there.

Per D-05 and D-08, do not add person filters, a new identity surface, responsive rules, or final hierarchy work. Per D-09 and D-11, do not change config selection, the legacy render branch, or custom-element registration. Per D-12 and D-15, keep every geometry correction under `.moran-wall-shell`; `src/ha-family-board-card.ts` must remain unchanged. Do not touch calendar state, event data, Home Assistant API calls, localization, dependencies, or any file under `/Volumes/config`.</action>
  <verify>
    <automated>npm run format:check &amp;&amp; npm run lint &amp;&amp; npm test &amp;&amp; rg -n "moran-wall-shell.*phead|aspect-ratio: 1 / 1|flex-shrink: 0|grid-template-columns|pbadges" src/wall-shell.ts &amp;&amp; git diff --quiet -- src/ha-family-board-card.ts &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Wall Day avatar geometry explicitly prevents one-axis flex shrink while the shared `.avatar` rule and all context-specific legacy/view sizing remain unchanged.
    - A visible wall person header uses explicit avatar, name/status, and optional-badge tracks within the unchanged 80px outer height.
    - The wall header with the optional badge and the three headers without badges have no layout overflow by construction.
    - Collapsed wall headers still fit the avatar inside the inherited 48px lane width.
    - No DOM, event handler, calendar/config logic, dependency, responsive contract, or live Home Assistant file changes.
  </acceptance_criteria>
  <done>Wall-scoped CSS establishes square, non-shrinking Day avatars and an 80px wall header capable of containing the fixture's optional 48px badge without altering shared legacy geometry.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce wall and legacy avatar geometry in the compiled browser harness</name>
  <files>dev/harness.html, dev/harness-check.mjs, dist/moran-family-board-card.js</files>
  <read_first>
    - `src/ha-family-board-card.ts`
    - `src/wall-shell.ts`
    - `dev/harness.html`
    - `dev/harness-check.mjs`
    - `.planning/phases/01-prove-the-foundation/01-03-SUMMARY.md`
    - `.planning/phases/01-prove-the-foundation/01-VALIDATION.md`
    - `rollup.config.js`
  </read_first>
  <action>Extend the existing page-side browser checks in `dev/harness.html` before the legacy early return. Measure every visible Day person-header avatar with `getBoundingClientRect()` and fail if the scenario does not render all four fixture avatars, if either dimension is zero, or if `Math.abs(width - height)` exceeds a 0.5px device-pixel tolerance. Assert the exact established baselines: 40x40px in wall mode and 34x34px in legacy mode. For wall mode, also assert every visible `.phead` remains 80px high and has `scrollWidth <= clientWidth + 1` and `scrollHeight <= clientHeight + 1`; explicitly assert that the first header contains the optional `.pbadge`, that this badge remains at least 48x48px, and that at least one other header exercises the no-badge path. Keep the existing independent badge/header keyboard and pointer checks intact. Add a stable, generic geometry marker to each successful result string.

Update `dev/harness-check.mjs` so each scenario declares its expected geometry marker and the headless runner refuses a nominal `pass` result that does not contain that marker. Retain the 1920x1080 viewport, America/Chicago timezone, loopback-only server, wall interaction run, and legacy root smoke behavior. This makes `npm run test:harness` prove both the UAT correction and the legacy baseline rather than accepting a skipped geometry branch.

Per D-16, review the handwritten source and harness diff, then regenerate `dist/moran-family-board-card.js` only with `npm run build`; never edit the minified file directly. Per D-17 through D-20, keep the existing deterministic Avery/Jordan/Casey/Household fixture and `fixture_*` entity IDs and access no live Home Assistant API or filesystem. Run build before `npm run test:harness` so the geometry gate exercises the corrected production bundle. Task 3 records and scans the completed execution evidence after these measurements exist.</action>
  <verify>
    <automated>npm run format:check &amp;&amp; npm run lint &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run test:harness &amp;&amp; rg -n "40x40|34x34|0\.5|expected.*geometry|geometry.*marker" dev/harness.html dev/harness-check.mjs &amp;&amp; git diff --quiet -- package.json package-lock.json &amp;&amp; git diff --check</automated>
    <human_check>Open the compiled wall and legacy harness scenarios at 1920x1080. Confirm all four wall avatars are circular, the first wall header's 48px badge and its identity text fit inside the unchanged 80px row, the other wall headers are not distorted, and legacy retains its familiar 34px circular avatars and card shell.</human_check>
  </verify>
  <acceptance_criteria>
    - `npm run test:harness` fails for a non-square wall or legacy avatar and reports stable evidence for 4x40x40 wall and 4x34x34 legacy geometry when passing.
    - The badge-bearing wall header and no-badge wall headers remain within their 80px boxes with no scroll overflow.
    - Existing badge/header pointer, Enter, Space, focus, and 48px-target assertions remain green.
    - The harness still imports and exercises only the Rollup-generated production bundle through generic mocked Home Assistant data.
    - The dependency manifests are unchanged, the full quality gate passes, and implementation files contain no live Home Assistant path or private fixture data.
  </acceptance_criteria>
  <done>The UAT failure is reproducibly closed in the compiled wall scenario while the same automated run proves legacy avatar geometry and behavior remain intact.</done>
</task>

<task type="auto">
  <name>Task 3: Record closure evidence and enforce the final privacy gate</name>
  <files>.planning/phases/01-prove-the-foundation/01-04-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/01-prove-the-foundation/01-VALIDATION.md`
    - `.planning/phases/01-prove-the-foundation/01-03-SUMMARY.md`
    - `dev/harness-check.mjs`
  </read_first>
  <action>Create the required `01-04-SUMMARY.md` before running this task's verification. Record the measured wall and legacy avatar dimensions, wall header and badge containment, browser viewport/timezone, quality-gate results, generated-bundle result, unchanged dependency manifests, and a truthful statement that no live Home Assistant file, dashboard, entity, API, calendar, or credential was read or changed.

Per D-19 and T-01-18, run the concrete denylist scan in `<verify>` across both relevant source files (`src/ha-family-board-card.ts` and `src/wall-shell.ts`), both harness files, the generated distribution bundle, and the completed `01-04-SUMMARY.md`. The scan must reject credential assignments/tokens/private keys and the established private household names/entity IDs. Keep `/Volumes/config` as a separate negative assertion over implementation, harness, and bundle files only because the summary must explicitly document that live boundary. Any match blocks completion; do not redact a finding and continue. Per D-20, do not access the live path to perform this verification.</action>
  <verify>
    <automated>test -f .planning/phases/01-prove-the-foundation/01-04-SUMMARY.md &amp;&amp; privacy_files=(src/ha-family-board-card.ts src/wall-shell.ts dev/harness.html dev/harness-check.mjs dist/moran-family-board-card.js .planning/phases/01-prove-the-foundation/01-04-SUMMARY.md) &amp;&amp; implementation_files=(src/ha-family-board-card.ts src/wall-shell.ts dev/harness.html dev/harness-check.mjs dist/moran-family-board-card.js) &amp;&amp; private_pattern='\b([M]atthew|[O]liver|[E]li|[J]uliet|[A]lex|[J]amie|[R]iley)\b|calendar\.(family|activities|anna|ben_work|ben_private|work)|person\.(alex|jamie|riley)|(?:HOME_ASSISTANT_TOKEN|SUPERVISOR_TOKEN)\s*[:=]\s*(?:\x22|\x27)?[^\s\x22\x27]{8,}|ghp_[A-Za-z0-9]{20,}|github_pat_[A-Za-z0-9_]{20,}|sk-[A-Za-z0-9_-]{20,}|AKIA[0-9A-Z]{16}|Bearer\s+[A-Za-z0-9._~-]{20,}|-----BEGIN (?:RSA |EC |OPENSSH )?PRIVATE KEY-----' &amp;&amp; ! rg -n --hidden --pcre2 "$private_pattern" "${privacy_files[@]}" &amp;&amp; ! rg -n --fixed-strings '/Volumes/config' "${implementation_files[@]}" &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The completed summary contains the measured geometry, containment, quality, build, dependency, privacy, and untouched-live-HA evidence.
    - The exact credential/private-identifier denylist scan covers both relevant source files, both harness files, generated `dist`, and the completed `01-04-SUMMARY.md`; every match fails the task.
    - The separate implementation-only live-path assertion passes without scanning governance text that intentionally names the boundary.
  </acceptance_criteria>
  <done>Gap-closure evidence exists and the final D-19/T-01-18 privacy gate passes against every required source, harness, bundle, and summary artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wall-shell CSS -> shared Day person-header markup | A wall-only geometry correction can leak into other views or the no-layout legacy path if it is not rooted beneath `.moran-wall-shell`. |
| Wall person-header contents -> fixed 80px layout box | User-configured identity text and optional focusable badges can force clipping or overflow. |
| Browser fixture -> public repository and test output | Test data and diagnostics can disclose private household information if generic boundaries regress. |
| Reviewed TypeScript/CSS -> generated HACS bundle | The shipped minified artifact must correspond to reviewed source rather than a manual bundle edit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Tampering | Wall Day `.phead .avatar` geometry | mitigate | Put shrink/aspect-ratio geometry only under `.moran-wall-shell`, leave the shared `.avatar` rule unchanged, and assert exact 40x40 wall plus 34x34 legacy Day baselines. |
| T-01-16 | Denial of Service | Fixed-height wall `.phead` | mitigate | Allocate explicit wall-only grid tracks, collapse the absent badge track, ellipsize text, preserve collapsed-lane fit, and assert client/scroll containment with the 48px badge present. |
| T-01-17 | Repudiation | Browser geometry evidence | mitigate | Require scenario-specific markers from actual DOMRect measurements in both headless Chrome scenarios before the runner reports success. |
| T-01-18 | Information Disclosure | Fixture, logs, generated bundle, and execution summary | mitigate | Keep generic fixture identities, log dimensions/classes only, create `01-04-SUMMARY.md`, then run the explicit negative credential/private-identifier `rg --pcre2` scan across both relevant source files, both harness files, generated `dist`, and that completed summary; separately reject the live path in implementation artifacts without accessing it. |
| T-01-19 | Tampering | `dist/moran-family-board-card.js` | mitigate | Build before browser verification through the locked Rollup command and prohibit direct edits to the bundle. |
| T-01-SC | Tampering | npm package supply chain | accept | The diagnosed CSS/harness correction needs no new package; any manifest or lockfile change blocks completion. |
</threat_model>

## Source Coverage Audit

| Source | Gap-closure coverage |
|--------|----------------------|
| GOAL | Restores credible person identity in the opt-in wall Day slice while retaining the safe legacy comparison required by the Phase 1 goal. |
| REQ | FOUND-02 is directly protected: exact Wall gets the corrected geometry and no-layout legacy retains its established result. FOUND-01 and FOUND-03 already passed and are not reopened. |
| RESEARCH | Uses the existing Lit scoped-CSS and Chrome harness patterns, adds no framework/package/database/provider, and regenerates the committed Rollup artifact. |
| CONTEXT | Implements D-05, D-08, D-09, D-11, D-12, D-15, D-16, D-17, D-18, D-19, and D-20; the remaining Phase 1 decisions are already satisfied by Plans 01-01 through 01-03 and are unaffected. |

Deferred person filters, final now/next hierarchy, dense-day redesign, tablet/portrait behavior, weather, failure-state work, household operations, and live deployment remain excluded exactly as assigned to later phases.

## Artifacts this plan produces

- Updated `src/wall-shell.ts` — wall-only square avatar geometry and 80px grid layout accommodating avatar, name/status, and optional 48px badge; the shared unscoped avatar rule remains unchanged.
- Updated `dev/harness.html` — rendered avatar and header-containment assertions for both scenarios.
- Updated `dev/harness-check.mjs` — scenario-specific geometry-evidence enforcement.
- Regenerated `dist/moran-family-board-card.js` — HACS-compatible artifact produced from source.
- `.planning/phases/01-prove-the-foundation/01-04-SUMMARY.md` — closure evidence.

<verification>
- `npm run format:check &amp;&amp; npm run lint &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run test:harness` passes in that order.
- Headless Chrome reports four 40x40 wall avatars, four 34x34 legacy avatars, an 80px contained wall header, and a contained 48px badge at 1920x1080 America/Chicago.
- Wall badge/header interaction checks and legacy root checks remain green.
- `package.json` and `package-lock.json` remain unchanged; `dist` is regenerated only by Rollup.
- After `01-04-SUMMARY.md` exists, the explicit negative `rg --pcre2` credential/private-identifier scan passes across `src/ha-family-board-card.ts`, `src/wall-shell.ts`, both harness files, generated `dist`, and the completed summary; the separate implementation-only scan finds no `/Volumes/config` reference.
- No live Home Assistant file, dashboard, entity, API, calendar, or credential is read or changed.
</verification>

<success_criteria>
- The sole diagnosed UAT gap is closed with a rendered, automatically enforced square-avatar result.
- The original 80px wall person-header and 48px interaction contracts coexist without clipping or overflow.
- Legacy Day visual geometry and behavior remain explicitly proven by the same production bundle, with no shared avatar-rule edit that could require broader legacy-view regression coverage.
- FOUND-02 remains satisfied and no Phase 2+, dependency, provider, or live-deployment scope enters Phase 1.
- No high-severity threat remains unmitigated.
</success_criteria>

<output>
Create `.planning/phases/01-prove-the-foundation/01-04-SUMMARY.md` when done.
</output>
